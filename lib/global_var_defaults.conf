
# This file contains default values for global variables.

: ${shew__architecture:=}				# The architecture of the system being installed.
: ${shew__current_script:=}				# The path to the current running script.
: ${shew__fixit_shewstring_installer_dir:=}		# The path to the installer when using fixit.
: ${shew__freebsd_version:=}				# The version of the system being installed.
: ${shew__shewstring_installer:=}			# Which Shewstring installer to use.
: ${shew__using_fixit:=}				# Whether the installer is using fixit or not.
